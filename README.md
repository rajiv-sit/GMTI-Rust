# Advanced GMTI Rust Platform

This directory documents the Rust-based redesign of the legacy AESADIRP/AIRRADAR stack.

See `architecture.md` for the full module layout and `requirements.md` for functional needs.
